# Review learning record: 896F5F8050686A23

- Schema: `review-learning/v1`
- Date (UTC): 2026-07-31
- Source policy: `change-risk/v2`
- Product version: 0.5.0
- Base: `9e5e7222dd7a9ca91c55ac815143bde28e81b0f1`
- Head: `896F5F8050686A23`
- Worktree snapshot: `896F5F8050686A23`
- Reviewer surface: change-risk-reviewer subagent, Claude Code
- Reviewer surface version: unknown
- Logical invocations: 3
- Transient attempts: 2
- Terminal status: `clean`

Reviewed change: phase-33 I8 (budget exhaustion degrades to `NEEDS_CONTEXT`)
and I9 (mechanical guard for structured envelope field shapes), on branch
`phase-33-i8-i9-reviewer-prompt-guards`. The head is a worktree snapshot; no
commit existed when the review reached a terminal status.

## Corrections (2026-08-01, superseding)

Three claims below are superseded. They are struck here rather than rewritten,
because promotion and I6 consume this record and not the ledger narrative where
the corrections were originally written.

1. **The transient attempts are not evidence about I8.** The paragraph under
   Rounds calls them "the exact behaviour I8 exists to remove, observed while
   reviewing I8". Those invocations ran against this repository's own stale
   `.claude/agents/change-risk-reviewer.md`, so the prompt in effect was the
   pre-I8 one and the rule was never present to fire. I8 is UNEXERCISED, not
   demonstrated and not failed. There are zero reviews under the post-I8
   prompt. I6 must treat the first such review as the initial data point and
   must not read these runs as evidence about I8 in either direction.

2. **`cross-consumer-integration` is not a validated second occurrence.** The
   promotion section records PR #140 as a first occurrence by owner
   determination, owed to the I5 backfill. Both halves fail: the parent
   contract requires promotion to count changes with validated findings and to
   read persisted values, "never re-adjudicating prose", so a prose assertion
   cannot authorize a threshold; and I5 is scoped to PRs #125 and #127-#133 by
   an explicit non-goal, so it neither owns nor will supply #140. Treat #140 as
   context only, carrying no promotion authority, and do not count it.

3. **The elected guard's exclusion list is wrong.** The promotion section
   excludes `.claude/settings.json` and `.mcp.json` alike. They are not
   equivalent: `.claude/settings.json` is tracked, emitted by the
   `claude-settings` target and `generated-owned` in `ai-profile.lock`, so
   exempting it would let a stale generated-owned artifact pass the very gate.
   The guard compares committed bytes and tolerates a working-copy override
   separately; `.mcp.json` is gitignored and untracked, so it has no committed
   artifact and is out of scope. The guard as filed (phase-33 I14) is also
   framed as an early mechanical guard under the guard-preference clause rather
   than as a second-occurrence outcome, and must additionally detect orphaned
   artifacts, since a dry run reports no delete category and compile never
   removes orphans.

## Rounds

| Round | Source | Blockers | Clusters formed | Cluster recurrence |
| ----- | ------ | -------- | --------------- | ------------------ |
| 1     | local  | 1        | -               | none               |
| 2     | local  | 0        | -               | none               |
| 3     | local  | 0        | -               | none               |

Round 1 was the initial clean-room review, round 2 the remediation review
after the single fix round, round 3 the final clean-room confirmation required
because the change touches generated ownership. No cluster of three or more
findings sharing a cause formed, so no cluster-recurrence guard was owed.

Both transient attempts were the same failure: the reviewer spent its budget
inspecting and returned no envelope, on the initial review and again on the
final confirmation. Each was recorded as an invalid attempt and retried by
resuming the same invocation, which then produced a well-formed envelope. This
is the exact behaviour I8 exists to remove, observed while reviewing I8.

## Findings

| Priority | Category                   | Contract                   | Source | Resolution | Disposition | Safe path                                                                             |
| -------- | -------------------------- | -------------------------- | ------ | ---------- | ----------- | ------------------------------------------------------------------------------------- |
| P2       | cross-consumer-integration | generated-region-ownership | local  | fixed      | -           | Regenerate the self-applied root artifacts and lock alongside the fixture goldens.    |
| P3       | parser-version-contract    | contract-completeness      | local  | fixed      | fixed       | State the derivation guard's real coverage boundary in both the spec and the comment. |
| P3       | cross-consumer-integration | contract-completeness      | local  | open       | follow-up   | Make the reviewer's remaining turn budget observable, or reword the rule budget-free. |
| P3       | parser-version-contract    | parsing-validation         | local  | open       | follow-up   | Project the interior constraints for the `location` keys the prompt now advertises.   |

Category taxonomy version: `change-risk/v2`.

Disposition confirmations (owner): both open P3 findings were verified against
the code by the orchestration owner and confirmed `follow-up`. Neither is in
scope for the reviewed change — I8 names programmatic budget detection an
explicit non-goal, and I9 is scoped to field shape rather than interior value
constraints — and both are filed as dispatchable issue briefs, so deferring
them loses nothing.

## Evidence

1. **P2, `cross-consumer-integration` / `generated-region-ownership`,
   `fixed`.** Fingerprint
   `cross-consumer-integration|generated-region-ownership|.claude/agents/change-risk-reviewer.md|incomplete-propagation`.
   The repository self-applies its own generated reviewer artifacts. The change
   advanced the golden fixtures but left the checked-in root artifacts and root
   `ai-profile.lock` on the pre-change bytes, so the new guards never reached
   the reviewer this repository actually runs — including the review that found
   it. No existing gate detects the divergence: `npm run check`, `npm test`, and
   `npm run verify:pack` all passed while the root artifacts were stale.
   Evidence: root `ai-profile.lock` change-risk-reviewer entries versus the
   fixture lock entries; the root artifact hashes before and after
   regeneration. Fixed by regenerating the root artifacts and lock in the same
   change; the root and fixture artifacts are now byte-identical.

2. **P3, `parser-version-contract` / `contract-completeness`, `fixed`.**
   Fingerprint
   `parser-version-contract|contract-completeness|docs/specs/phase-33/001-change-risk-review-assurance.md|other`.
   The parent spec and the renderer comment claimed the derived guard prevents
   any newly structured field from reaching the prompt unstated. The guard
   scalar-substitutes each record-valued path of canonical envelopes, so it
   sees only fields those envelopes carry: it covers newly _required_
   structured fields and not newly _optional_ ones. Evidence: the added spec
   sentence, the `structuredShapes` renderer comment, and the derivation loop
   in `packages/compiler/src/compiler.test.ts`. Fixed by qualifying both
   statements to name the real boundary.

3. **P3, `cross-consumer-integration` / `contract-completeness`, `open`,
   `follow-up`.** Fingerprint
   `cross-consumer-integration|contract-completeness|packages/compiler/src/change-risk-policy.ts#budgetDegradationRules|incomplete-propagation`.
   The projected budget rules condition on "the remaining turn budget", a
   quantity the reviewer is never told in prose. On the Claude target the
   budget appears only as `maxTurns` frontmatter, which is harness
   configuration rather than instruction text; the Codex agent surface emits no
   turn-budget key at all, so the Codex reviewer has no declared budget on any
   surface. Evidence: the projected rules; the emitted Codex artifact's
   top-level keys; `renderCodexSubagent` versus `renderClaudeSubagent`.
   Confirmed against the artifacts by the owner. Deferred: I8 names programmatic
   budget detection an explicit non-goal, and I6 owns measuring whether the
   prompt rule alone suffices. Filed as phase-33 I10
   (`issues/010-reviewer-budget-observability.md`) in the separate ledger
   change, so this reviewed change stays the snapshot that was confirmed.

4. **P3, `parser-version-contract` / `parsing-validation`, `open`,
   `follow-up`.** Fingerprint
   `parser-version-contract|parsing-validation|packages/compiler/src/change-risk-policy.ts#structuredFieldShapes|missing-validation`.
   The shape list now advertises `findings[].location.line` with no validity
   rule, while the sibling `findings[].evidence[].lines` keeps its explicit
   `1 <= start <= end` rule. `validateFinding` rejects a non-integer or `< 1`
   `line`, a `path` containing a `..` segment, and a `path` normalizing to no
   component — none of which the prompt states. Evidence: the projected
   `structuredFieldShapes` entry; `validateFinding`; the emitted prompt's
   asymmetric treatment of the two locator shapes. Confirmed against the
   validator by the owner. Deferred: I9 is scoped to field shape, not interior
   value constraints. Filed as phase-33 I11
   (`issues/011-reviewer-locator-value-constraints.md`) in the separate ledger
   change.

## Promotion

Recurrence is keyed on the canonical category and the occurrence unit is one
reviewed change, so this record contributes exactly one occurrence of
`cross-consumer-integration` and one of `parser-version-contract`, not two of
each.

`parser-version-contract` is at its first occurrence in this taxonomy: record
and categorize only. Neither P1 nor any systemic predicate applies.

`cross-consumer-integration` is at its **second** occurrence by owner
determination. The corpus in `docs/review-learning/` holds no prior record — the
first occurrence was the half-emitted reviewer during PR #140, whose reviews
were ad hoc with no orchestration behind them and which is therefore owed to
the I5 backfill rather than derivable from this directory. The owner supplied
that occurrence with evidence; it is recorded here as an owner determination
rather than a derived count, so I5 can reconcile it and must not double-count
the #140 event when the backfill record lands.

Both occurrences are the same mechanism: the repository's own generated
artifacts go stale, so a fix that exists is not in effect for the agent
actually running. Under the promotion table a second occurrence earns a
reviewer regression case plus a scoped rule, and the table explicitly permits
a mechanical guard earlier when clearly practical and proportionate. The owner
elected the guard: assert that `compile --dry-run` reports zero create/change
for this repository's own artifacts, excluding the two intentionally-local
files (`.claude/settings.json`, `.mcp.json`). It is mechanical, cheap, and
would have caught both occurrences. Filed as phase-33 I14 in the separate
ledger change; it is not implemented here, because implementing it would
change the snapshot this record certifies.

### Coverage note for I6

I9's derived guard covers the _shape_ of structured envelope fields. It does
not cover _completeness_ of a field list: the "Each finding contains exactly
\<ten fields\>" error, which omitted the conditionally-required `disposition`,
was caught by a human and not by the mechanism. I6 must not credit I9 with
preventing that class. Extending the guard to assert every conditionally
required field is named — the same derive-from-the-validator technique — is
filed as phase-33 I13.
