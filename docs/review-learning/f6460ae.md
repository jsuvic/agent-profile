# Review learning record: f6460ae

- Schema: `review-learning/v1`
- Date (UTC): 2026-08-01
- Source policy: `change-risk/v2`
- Product version: 0.5.0
- Base: `775ad9d`
- Head: `f6460ae`
- Worktree snapshot: `f6460ae`
- Reviewer surface: change-risk-reviewer subagent
- Reviewer surface version: unknown
- Logical invocations: 2
- Transient attempts: 0
- Terminal status: `no-progress`

The initial clean-room review ran against snapshot `3da9fb` and returned three
P2 blockers. One fix round produced snapshot `f6460ae`; remediation closed two
findings, left the fingerprint-identity finding open, and introduced a new
cumulative-promotion finding. The repeated open fingerprint produced terminal
`NO_PROGRESS`. No confirmation review ran, and no further review is authorized
for this terminal snapshot.

## Rounds

| Round | Source | Blockers | Clusters formed                                                                      | Cluster recurrence |
| ----- | ------ | -------- | ------------------------------------------------------------------------------------ | ------------------ |
| 1     | local  | 3        | -                                                                                    | none               |
| 2     | local  | 2        | parsing-validation+missing-validation, contract-completeness+incomplete-propagation | none               |

The round-2 cluster keys are unresolved human-escalation obligations. `none`
records that no guard was completed or proved impractical; it is not evidence
that either obligation disappeared. The proposal cited below is not applied.

## Findings

| Priority | Category                | Contract              | Source | Resolution | Disposition | Safe path                                                                                                                                             |
| -------- | ----------------------- | --------------------- | ------ | ---------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| P2       | parser-version-contract | parsing-validation    | local  | open       | -           | Use a real source symbol when known; otherwise omit it and use a separate stable sanitized defect discriminator, with a wording-only mutation test.   |
| P2       | runtime-proof           | runtime-proof         | local  | fixed      | -           | Keep finding-specific precondition, unsafe-behavior, consequence, and safe-path summaries with focused anti-template tests.                           |
| P2       | state-classification    | contract-completeness | local  | fixed      | -           | Keep recurrence aggregated by distinct reviewed change and assert exact shared-policy action parity.                                                  |
| P2       | state-classification    | contract-completeness | local  | open       | -           | Emit cumulative earned actions: retain systemic-P1 and second-occurrence protections, then add the third-occurrence guard instead of replacing them. |

## Evidence

### `["parser-version-contract","parsing-validation",{"path":"docs/review-learning/historical-corpus.json","symbol":"records[].record.findings[].fingerprint","line":null},"missing-validation"]`

- `docs/review-learning/historical-corpus.json:40`, symbol
  `records[].record.findings[].fingerprint`: the canonical helper receives a
  synthetic `review:*` location symbol derived from remediation wording, so a
  wording-only change alters identity. This remained open after remediation.

### `["runtime-proof","runtime-proof",{"path":"docs/review-learning/historical-corpus.json","symbol":"records[].record.findings[].sanitizedSummary","line":null},"missing-runtime-proof"]`

- `records[].record.findings[].sanitizedSummary` now carries sanitized,
  finding-specific precondition, unsafe behavior, consequence, and safe path;
  focused tests reject generic templates. Remediation verified this fixed.

### `["state-classification","contract-completeness",{"path":"docs/review-learning/generate-historical-corpus.mjs","symbol":"renderSummary","line":null},"incomplete-propagation"]`

- `renderSummary` now counts distinct pull-request records and projects exact
  shared-policy action text. Remediation verified the distinct-change and
  action-parity coverage fixed.

### `["state-classification","contract-completeness",{"path":"docs/review-learning/generate-historical-corpus.mjs","symbol":"promotionDecision","line":null},"incomplete-propagation"]`

- `docs/review-learning/generate-historical-corpus.mjs:115`, symbol
  `promotionDecision`: mutually exclusive selection retains only the highest
  action and drops protections earned at systemic-P1 and second-occurrence
  thresholds. This cumulative-action finding is open.

## Promotion

The occurrence unit is one reviewed change. Multiple rounds and fingerprints
in this record collapse to at most one occurrence per canonical category.

- The fixed `runtime-proof` finding contributes one validated occurrence.
- The fixed `state-classification` finding contributes one validated
  occurrence. The second open finding in that category does not add another.
- The open `parser-version-contract` and `state-classification` P2 findings do
  not count because neither carries `dispositionConfirmed` plus owner decision
  evidence.

Against the checked-in historical corpus, this change takes `runtime-proof`
from two reviewed changes to its third and `state-classification` from four to
five. The applicable third-occurrence action is: add a test, lint, validator,
or shared helper where practical; the prompt rule alone has proven
insufficient. This record does not claim that action completed.

The validated orchestration state requires human resolution of two mechanical
guard obligations:

- `parsing-validation+missing-validation`: stabilize fingerprint identity
  against wording-only changes.
- `contract-completeness+incomplete-propagation`: preserve cumulative
  promotion obligations rather than selecting only the highest action.

A concise proposed patch is recorded at
`docs/review-learning/proposals/f6460ae-no-progress-guards.md`. It is proposal
evidence only; no generated or human-owned rule was modified.
