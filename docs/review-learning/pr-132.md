# Historical review learning record: PR #132

- Schema: `review-learning/v1`
- Date (UTC): 2026-07-24
- Source policy: `legacy-external`
- Base: `9f434906bfd8`
- Head: `f31565796670`
- Reviewer surface: GitHub pull-request review threads (sanitized historical snapshot)
- Reviewer surface version: unknown
- Terminal status: `external-only`
- Approved snapshot: 2 findings (1 P1, 1 P2).
- Later observation (2026-08-01): 2 threads, 2 outdated; this does not replace the approved snapshot.

## Rounds

| Round | Source | Blockers |
| --- | --- | ---: |
| 1 | external | 2 |

## Findings

| Priority | Category | Contract | Source / provider | Resolution | Safe path | Finding | Fingerprint | Sanitized evidence |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| P1 | parser-version-contract | parsing-validation | external (github) | fixed | Carry the preset through the final YAML render. | pr-132#thread-1 | ["parser-version-contract","parsing-validation",{"path":"apps/web/src/lib/profileEditor.ts","symbol":null,"line":null},"incomplete-propagation"] | Precondition: When a v3 profile is saved. Unsafe behavior: This carries rawSubagentPolicy.preset into the candidate, but /api/profile/plan subsequently calls renderProfileYaml, whose buildSubagentPolicyDoc never emits policy.preset. Consequence: The reviewed YAML as a result removes preset, silently downgrading the profile to mapping-v2 behavior despite this pass-through. Safe path: Carry the preset through the final YAML render. |
| P2 | secret-output | secret-handling | external (github) | fixed | Exclude unredacted model overrides from page data. | pr-132#thread-2 | ["secret-output","secret-handling",{"path":"apps/web/src/routes/profile/+page.server.ts","symbol":null,"line":null},"redaction-failure"] | Precondition: When a valid v3 exact override contains a secret-like literal (the open override validator permits any non-control string up to 200 characters). Unsafe behavior: Returning the entire raw policy here serializes that literal into the browser page data even though yamlRedacted correctly replaces the YAML preview with «redacted». Consequence: Preserve this uneditable block server-side for the save plan, or block/redact the page state without embedding the raw value in the response. Safe path: Exclude unredacted model overrides from page data. |

## Evidence

### `pr-132#thread-1`
- Fingerprint: `["parser-version-contract","parsing-validation",{"path":"apps/web/src/lib/profileEditor.ts","symbol":null,"line":null},"incomplete-propagation"]`
- Precondition: When a v3 profile is saved. Unsafe behavior: This carries rawSubagentPolicy.preset into the candidate, but /api/profile/plan subsequently calls renderProfileYaml, whose buildSubagentPolicyDoc never emits policy.preset. Consequence: The reviewed YAML as a result removes preset, silently downgrading the profile to mapping-v2 behavior despite this pass-through. Safe path: Carry the preset through the final YAML render.

### `pr-132#thread-2`
- Fingerprint: `["secret-output","secret-handling",{"path":"apps/web/src/routes/profile/+page.server.ts","symbol":null,"line":null},"redaction-failure"]`
- Precondition: When a valid v3 exact override contains a secret-like literal (the open override validator permits any non-control string up to 200 characters). Unsafe behavior: Returning the entire raw policy here serializes that literal into the browser page data even though yamlRedacted correctly replaces the YAML preview with «redacted». Consequence: Preserve this uneditable block server-side for the save plan, or block/redact the page state without embedding the raw value in the response. Safe path: Exclude unredacted model overrides from page data.
